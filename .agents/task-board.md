# 📋 Kanban Доска Проекта: Liman Web API

Синхронизируется с Trello и используется для координации AI-агентов и разработчиков.

---

## 🎯 Колонка: To Do (Спринт 2: Следующие интеграции / На очереди)
- [ ] **[TASK-31]** Подключение WooCommerce к единому движку заказов Limansoft:
  - **Статус готовности**: 🟢 Готов к разработке (использует общий движок из TASK-29);
  - **Приоритет**: 🔴 **Высший (Очередь #1 — Следующая задача)**;
  - **Подзадачи**:
    - Переключение `handleOrderWebhook` и `syncOrders` WooCommerce на единый метод `processIncomingOrder`;
    - Радиокнопка/тумблер `woocommerceCreateOrderDocumentEnabled` в настройках тенанта и UI (по умолчанию: **выключено**, пометка: «Экспериментальная функция»);
    - Унификация маппинга полей заказа WooCommerce (Billing, Shipping, Line Items, Payment Gateway);
    - Автоматическая фиксация источника «WooCommerce» в накладной.
- [ ] **[TASK-30]** CI/CD пайплайн GitHub Actions и подготовка продакшн-деплоя на Hetzner:
  - **Статус готовности**: 🟢 Готов к пушу (Dockerfile, deploy.yml, deploy.sh, compose.prod готовы);
  - **Приоритет**: 🔴 **Высший (Очередь #2)**;
  - **Подзадачи**:
    - Написание оптимизированного `Dockerfile` (multi-stage сборка NestJS + React SPA);
    - Создание GitHub Actions workflow `.github/workflows/deploy.yml` (триггер по push в `main` / `master`, тесты, сборка образа в `ghcr.io`, деплой через SSH `appleboy/ssh-action`);
    - Скрипт автоматического обновления на сервере `scripts/deploy.sh` с healthcheck и ротацией образов;
    - Конфигурация `docker-compose.prod.yml` для работы на одном Hetzner-сервере рядом с `restaurantify` без конфликта портов;
    - Инструкция по настройке секретов GitHub (`HOST`, `USERNAME`, `SSH_KEY`) и домена.

---

## 📌 Колонка: Backlog (Бэклог)
- [ ] **[TASK-17]** Выгрузка товаров из магазина WooCommerce обратно в учетную БД Limansoft (MariaDB):
  - **Статус готовности**: 🟡 Ядро двухстороннего импорта реализовано в TASK-14 и TASK-24 (`class-migrator.php`, `class-direct-db.php`, `insertProductMaster`). Требуется доработка UI в плагине WordPress;
  - **Приоритет**: 🟡 **Средний (Очередь #2)**;
  - **Подзадачи**:
    - Карточка в админке WordPress плагина с кнопкой «📤 Вивантажити каталог до Limansoft»;
    - Выбор режима выгрузки («Тільки новинки» / «Повний каталог»);
    - AJAX progress-bar в реальном времени со счетчиками созданных/обновленных товаров;
    - Итоговый отчет о результатах и обработанных SKU.
- [ ] **[TASK-16]** Защита и лицензирование коммерческого плагина Limansoft Sync для WooCommerce:
  - **Статус готовности**: 🟢 Полное архитектурное ТЗ готово, требуется перед публичным релизом и продажей плагина;
  - **Приоритет**: 🟡 **Высокий (Коммерциализация, Очередь #3)**;
  - **Подзадачи**:
    - Расширение модели `Tenant` в SQLite (`licenseKey`, `licenseStatus`, `expiresAt`, `allowedDomains`, `plan`, `maxProducts`);
    - NestJS `LicenseGuard` и API эндпоинт `POST /api/v1/license/verify`;
    - Поле ввода и валидации лицензионного ключа в админке WordPress плагина;
    - Приватный сервер автоматических обновлений плагина (`Auto-Updater Engine`) в 1 клик прямо из панели WordPress.
- [ ] **[TASK-25]** Автономный Веб-Паук для поиска фото и обогащения описаний товаров в WooCommerce:
  - **Статус готовности**: ⚪ Базовый веб-скрапер Bing Images и Polylang генерация уже встроены в плагин в TASK-24 (`LSW_Web_Scraper`). Расширенная версия отложена в перспективный бэклог;
  - **Приоритет**: ⚪ **Низкий (R&D / На будущее, Очередь #4)**;
  - **Подзадачи**:
    - Фоновая очередь скрапинга через BullMQ в `liman-web-api`;
    - Подключение Google Custom Search API и DuckDuckGo с фильтрами качества;
    - Режим предварительной модерации найденных фото перед сохранением.

---

## ⚙️ Колонка: In Progress (В работе)
- *(Готово к запуску TASK-31)*

---

## 🔍 Колонка: In Review / QA (Тестирование и код-ревью)
- [x] Верификация Swagger UI: `http://localhost:3000/api/docs`
- [x] Верификация Prom XML фида: `http://localhost:3000/api/v1/prom/columb/feed.xml`
- [x] Верификация Rozetka XML фида: `http://localhost:3000/api/v1/rozetka/columb/feed.xml`
- [x] Верификация Horoshop XML фида: `http://localhost:3000/api/v1/horoshop/columb/feed.xml`
- [x] Верификация Horoshop Webhook заказа с авто-списанием остатка в MariaDB
- [x] Верификация WooCommerce REST API (Ping, Batch sync 5768 товаров, Auto-sync toggle)
- [x] WordPress плагин `limansoft-sync-woocommerce.zip` v1.1.0 с поддержкой Two-Way Sync и защитой от зацикливания
- [x] Верификация Telegram Alert Bot & Webhook с троттлингом: `POST /api/v1/alerts/test`
- [x] Верификация двустороннего импорта каталога WooCommerce → Limansoft (Pull + Push)
- [x] Верификация интеграций Prom.ua, Rozetka и WooCommerce в Личном Кабинете (TASK-21)

---

## ✅ Колонка: Done (Выполнено)
- [x] **[TASK-21]** Модули интеграций для маркетплейсов Prom.ua, Rozetka и WooCommerce в Личном Кабинете:
  - Витрина интеграций с переключением табов (Хорошоп, Prom.ua, Rozetka, WooCommerce) и бейджами («Подключено» / «Доступно для подключения» для апселла);
  - Вкладка Prom.ua: живой пинг API Prom, BullMQ запуск синхронизации остатков/цен, прямая ссылка на YML/XML фид с кнопкой копирования, ссылка на вебхук заказов, форма настройки API токена с безопасным раскрытием `👁`;
  - Вкладка Rozetka: проверка подключения к Seller API, кнопка триггера массового обновления остатков, XML фид прайс-листа, URL вебхука заказов, форма настроек Client ID / Secret;
  - Вкладка WooCommerce: проверка REST API, прямая кнопка 1-click скачивания плагина `limansoft-sync-woocommerce.zip` (эндпоинт `@Get('plugin/download')`), триггеры Push/Pull каталога и синхронизации остатков, вебхуки, форма параметров ck/cs и Two-Way Sync;
  - Полная двуязычная локализация (RU / UK) через `LanguageContext`;
  - 100% покрытие компонентными тестами Vitest (`MarketplacesTabs.test.tsx`: 50/50 passing) и бэкенд тестами Jest (157/157 passing).
- [x] **[TASK-29]** Единый изолированный движок заказов Limansoft MariaDB и интеграция Хорошоп:
  - Изоляция единой бизнес-логики заказов в `LimanOrderService` (`UnifiedIncomingOrderDto` как Anti-Corruption Layer);
  - Универсальный резолв товаров: строковые артикулы (напр. `ELE-23-0557`), штрихкоды (`strihcod`) и маппинги (`product_mappings`) ➔ `tcod` Лимана;
  - Два безопасных режима списания/фиксации:
    - Режим 1 (по умолчанию): мгновенный безопасный декремент остатка `name2ost.skl_k`;
    - Режим 2 (экспериментальный): создание черновика заказа покупателя `tip_dok: 85` (`prov: 'f'`), блокировка счетчика `ndok FOR UPDATE`, запись `checkdok`, строк `nakltelo` и аудит `dmonitor`;
  - Фиксация заказа №1 из Хорошоп в MariaDB и исправление фильтра статуса API (`stat_status: 1`);
  - Добавлен UI переключатель `horoshopCreateOrderDocumentEnabled` в Client Portal и SuperAdmin с бейджем «Экспериментально» и локализацией RU/UK;
  - 100% покрытие unit-тестами (Jest: 157 тестов, Vitest: 46 тестов).
- [x] **[TASK-28]** Связывание артикулов (ID) Limansoft с Хорошоп и выгрузка каталога с категориями:
  - Сущности `TenantIntegration` и `ProductMapping` в PostgreSQL (`liman_master`) с B-tree индексами;
  - Двустороннее сопоставление товаров (`limanTcod` <-> `externalArticle`) при импорте и экспорте;
  - Парсинг `response.log` Хорошопа (0 -> synced, ошибки -> error) и запись связей в `product_mappings`;
  - API статистики маппинга `GET /api/v1/horoshop/:tenantId/mappings/stats`.
- [x] **[TASK-26]** Динамическое название магазина Хорошоп, прямой экспорт каталога с фото/брендами/валютой и 503-резистентность:
  - Устранение хардкода `Columb Shop (Локальная MariaDB)` в шапке Клиентского портала;
  - Автоматическое подтягивание и сохранение реального названия магазина из Хорошоп (`horoshopShopTitle`) при ping и возможность ручного редактирования;
  - Прямой экспорт каталога в Хорошоп через фоновую очередь BullMQ `export-horoshop-catalog` (`HoroshopExportProcessor`) с троттлингом и пакетированием;
  - Динамическая загрузка дерева категорий Хорошоп через `/api/pages/export/` и выбор целевой/дефолтной категории (`defaultCategoryPath`) для новинок;
  - Доставка фото: динамический `publicBaseUrl` (`tenant.publicBaseUrl` / `https://liman.terrace.pp.ua`) и фикс Nginx роутинга (`location ^~ /api/` и `/media/`);
  - Обработка логов Хорошоп: поддержка кодов 22 (загрузка фото в CDN) и 28 (очистка галереи), разделение предупреждений шаблона (код 11) и фатальных ошибок (код 7);
  - Универсальный экспорт брендов (каскад: `name2.brand` -> ведущее слово -> `defaultBrand` в модалке без отраслевого хардкода) и валюты (`currency`, маппинг '00' -> 'UAH');
  - Отказоустойчивость к HTTP 503 Envoy Хорошоп: Retry с экспоненциальным бэкоффом (1с -> 2с -> 4с) и пакетный троттлинг 250мс в `syncPricesAndStocks`;
  - Модальное окно `HoroshopExportModal.tsx` с выбором режимов выгрузки, состава полей, селектором категорий, брендом по умолчанию, валютой и живым прогресс-баром;
  - Успешно развернуто и верифицировано на продакшн-сервере Hetzner (100/100 SKU выгружено без ошибок, фото успешно скачаны CDN Хорошопа).
- [x] **[TASK-27]** Вебхуки Хорошоп: авто-списание остатков при заказе и вебхук создания товара:
  - Тумблеры включения/выключения вебхуков в Клиентском портале и в панели SuperAdmin (`TenantModal.tsx`);
  - Вебхук заказа `POST /api/v1/horoshop/:tenantId/webhook/order` с проверкой активности флага и списанием остатков в MariaDB;
  - Вебхук товара `POST /api/v1/horoshop/:tenantId/webhook/product` для создания новинок;
  - Отображение ссылок на вебхуки с удобной кнопкой копирования в буфер обмена;
  - Полная мультиязычность (RU / UK) для всех новых контролов.
- [x] **[TASK-22]** Обратная синхронизация каталога из Хорошоп в учетную БД Limansoft (MariaDB):
  - Фоновый импорт каталога через BullMQ (`import-horoshop-catalog`) без риска таймаутов;
  - Два режима: «Только новинки» (безопасный режим) и «Полное обновление» (перезапись с валидацией);
  - Защитный барьер: предупреждающее модальное окно с обязательным чекбоксом осознания риска при перезаписи;
  - Гранулярные фильтры: выбор обновления розничных цен (`cena2`), складских остатков (`skl_k`), фото (`namedesc`);
  - Автоматическое создание бэкапа MariaDB перед импортом (`BackupService.createBackup`) и распределённый Redis lock (`lock:tenant:{id}:busy`);
  - Автономный планировщик `HoroshopSchedulerService` (автовыгрузка каждые 15/30/60 мин и регулярный опрос заказов с автосписанием остатков);
  - Автоматическое построение иерархии категорий в таблице `name` (`LimanService.resolveCategoryGroup`) и строгий протокол сессионной авторизации Хорошоп API (JSON-body token, запрет Bearer);
  - Асинхронная очередь `sync-stock` для Хорошоп с live progress (0-100%) в Client Portal;
  - 100% покрытие unit и компонентными тестами (Jest + Vitest).
- [x] **[TASK-24]** Объединение Columb DB Sync и Limansoft Sync в единый плагин WooCommerce с поддержкой двусторонней синхронизации:
  - Единый плагин `Limansoft Sync for WooCommerce` v2.0.0 (`limansoft-sync-woocommerce`);
  - Прямое подключение к MariaDB `columbDB` (`LSW_Direct_DB`) с автоопределением колонок цен и остатков;
  - Умная группировка товаров по брендам, вкусам, объему и крепости (`LSW_Product_Grouper`);
  - Двуязычные карточки Polylang UA/RU и Веб-паук скрапинга Bing Images (`LSW_Web_Scraper`);
  - Высокоскоростная синхронизация цен и остатков (3 секунды на 5900+ товаров) и очистка дубликатов (`LSW_Migrator`);
  - Обратная синхронизация: списание остатков при заказах и push-вебхуки при создании/редактировании товаров (`LSW_Webhook`);
  - Единая админка `WooCommerce -> Limansoft & Columb Sync` с живой консолью логов и статусами;
  - Сборка дистрибутива `packages/dist/limansoft-sync-woocommerce.zip` и развертывание в WP.
- [x] **[TASK-19]** Панель Супер-Админа: Сводная матрица всех клиентов, управление SQLite и монитор очередей:
  - Единая сводная интерактивная таблица (All-Tenants Master Grid) со всеми магазинами, настройками и статусами в одном окне;
  - Быстрые действия в строке: живой пинг MariaDB, модальное редактирование, Open Portal, копирование персональной ссылки клиента, ротация ключей, удаление;
  - Модалка создания/редактирования клиента с онлайн-тестом подключения к MariaDB Limansoft (мс, статус);
  - Маскирование паролей (`••••••••`) и безопасное раскрытие по клику `👁` с фиксацией в Security Audit Log (`admin-tenants.controller.ts`);
  - Монитор очередей BullMQ (`sync-stock`, `export-catalog`, `import-orders`, `import-woo-catalog`) со счетчиками задач и кнопкой `retry failed` (`admin-queues.controller.ts`);
  - Сводные SaaS-метрики (`/api/v1/admin/overview`);
  - Полная поддержка RU/UK и 100% прохождение тестов (Jest + Vitest).
- [x] **[TASK-20]** Личный кабинет клиента и модуль интеграции с Хорошоп (Horoshop Portal):
  - Диагностический визуальный блок «Светофор» (3-Point Health Bar: MariaDB, API Хорошоп, Автосинхронизация);
  - Action Hub: большая кнопка немедленной синхронизации цен и остатков (`syncPricesStocks`) с отчетом в реальном времени;
  - Блок ссылки на персональный XML-каталог фид с кнопками копирования в буфер обмена и открытия в новой вкладке;
  - Форма настройки параметров API-доступа (домен, логин, пароль с кнопкой `👁`, тумблер автосинхронизации, выбор интервала 15/30/60 мин);
  - Хронологическая лента событий синхронизации (Activity Feed) с API `GET /api/v1/horoshop/:tenantId/activity`;
  - 3-шаговый интерактивный мастер онбординга (`HoroshopWizard`);
  - Полная локализация RU/UK и тестирование Vitest (5 компонентных тестов).
- [x] **[TASK-18]** Развертывание React SPA каркаса (Vite + TypeScript), дизайн-система, i18n (RU/UK), Nginx статика и маршрутизация:
  - Инициализация `client/` на React + Vite + TS со сборкой в статику `public/app`;
  - Конфигурация Nginx Reverse Proxy для прямой отдачи статики (gzip, кэширование, 0% нагрузки на Node.js Event Loop);
  - Мультиязычность i18n (RU по умолчанию, автоопределение UK, localStorage);
  - Премиальная тёмная дизайн-система Limansoft Enterprise Dark;
  - Двухуровневая авторизация: Master API Key (Супер-Админ) vs Tenant Key (Кабинет Клиента);
  - Покрытие Vitest тестами (22 теста).
- [x] **[TASK-23]** Система резервного копирования и отката БД Limansoft (Backup, Restore & Safety Prompt):
  - Сервис создания сжатых `.sql.gz` дампов учетной базы тенанта (`name`, `name2`, `name2ost`, `namedesc`, `strihcod`);
  - Режимы Fast (каталог без фото, 1-3с) и Full (полный дамп с медиа);
  - Распределенная блокировка Redis Distributed Lock (`lock:tenant:{id}:busy`) с TTL;
  - Потоковая выборка батчами по 500 строк, Gzip и валидация контрольной суммы SHA-256;
  - Откат (восстановление базы) до любого сохраненного бэкапа в 1 клик с защитой от повреждения;
  - Скачивание архивов бэкапов и автоматическая ротация старых копий (10 копий, 30 дней);
  - Покрытие Jest тестами (24 теста).
- [x] **[TASK-14]** Двусторонний импорт каталога из WooCommerce в Limansoft MariaDB (Two-Way Sync):
  - Атомарный `upsertProductFromExternal` в `name2`, `name2ost`, `namedesc`, `strihcod` с `MAX(tcod)+1` для новых товаров;
  - Сопоставление товаров по SKU (`tcod`) и штрихкоду (`strihcod`/`nnom`);
  - Скачивание всех изображений из WooCommerce и сохранение в `namedesc` (BLOB);
  - Эндпоинты `POST /woocommerce/:tenantId/import/products` (Pull) и `POST /woocommerce/:tenantId/webhook/product` (Push);
  - Обновление WordPress-плагина `limansoft-sync` (v1.1.0): тумблер авто-обновления в админке, хуки `woocommerce_new_product` / `woocommerce_update_product`, защита от зацикливания через transients;
  - Документирование потоков данных и Sequence-диаграмма в `HOWITWORKS.md`.
- [x] **[TASK-15]** Webhook notifications & Telegram alert bot для критических ошибок синхронизации (Telegram Bot API HTML в группу, Generic Webhook JSON, Anti-spam throttling с TTL, алерты с MariaDB, BullMQ, WooCommerce, Rozetka, Horoshop, AllExceptionsFilter)
- [x] **[TASK-13]** Интеграция с Хорошоп (Horoshop / Cartum API: потоковый XML фид `/horoshop/:tenantId/feed.xml`, API клиент с JWT/session auth, синхронизация цен/остатков, webhook заказов с автосписанием)
- [x] **[TASK-11]** Интеграция с Rozetka Marketplace API (XML фид `/rozetka/:tenantId/feed.xml`, Seller API клиент с JWT-авторизацией, обновление остатков/цен, webhook заказов с автосписанием)
- [x] **[TASK-12]** Интеграция с WooCommerce REST API (Batch upsert с автоматическим определением create/update по SKU, delta sync, webhook заказов, отдельный WP плагин с UI и настраиваемым cron-расписанием)
- [x] **[TASK-01]** Инициализация NestJS 11, TypeScript, Swagger OpenAPI (`/api/docs`), модульная Clean Architecture
- [x] **[TASK-02]** Инфраструктура: Docker Compose с Redis для очередей BullMQ, проверено подключение
- [x] **[TASK-03]** Модуль мультиарендности (`TenantModule`) + локальная SQLite БД клиентов с авто-сидингом тенанта `columb`
- [x] **[TASK-04]** Data Access Layer для Limansoft MariaDB (`columbDB`: `name`, `name2`, `name2ost`, `namedesc`, `strihcod`, `dmonitor`). 5 768 активных SKU
- [x] **[TASK-05]** Сервис изображений (`MediaModule`): извлечение BLOB из `namedesc`, ETag, HTTP 304 кэширование и SVG placeholder
- [x] **[TASK-06]** Движок очередей BullMQ: очереди `sync-stock`, `export-catalog`, `import-orders`, отслеживание прогресса и API статуса
- [x] **[TASK-07]** Клиент Prom.ua REST API (`PromApiClient`) с типизацией и пакетными запросами
- [x] **[TASK-08]** Потоковый генератор Prom.ua YML/XML фида (`/api/v1/prom/columb/feed.xml`) — выгружает 5 768 товаров за ~430ms с минимальным потреблением RAM
- [x] **[TASK-09]** Фоновый синхронизатор остатков и цен через BullMQ (`StockSyncProcessor`)
- [x] **[TASK-10]** Webhook приема заказов из Prom.ua (`/api/v1/prom/columb/webhook/order`) с автоматическим списанием остатков в `name2ost`
