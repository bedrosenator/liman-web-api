# TASK-40: Исправление дефектов UI клиентского портала (бордеры табов, аккордеонов, бейджи, переполнение health-cards и полей)

- **ID:** TASK-40
- **Эпик:** Client Portal UI/UX Polish & Design Hygiene
- **Статус:** Completed
- **Приоритет:** High
- **Исполнитель:** Frontend & UI/UX Engineer

---

## 🎯 Цели задачи

1. **Устранение вылезания кнопки «Проверить связь» за пределы карточки (`health-card`)**:
   - В блоке «API Prom.ua» (а также Rozetka, WooCommerce) заменить текстовую кнопку с длинным названием `Проверить связь` на компактную кнопку-иконку `.btn-icon.btn-icon--sm` с индикатором загрузки и подсказкой `title={t('checkConnection')}`.
   - Защитить `.health-card` (`min-width: 0; overflow: hidden;`) и `.health-card__info` (`min-width: 0; flex: 1;`), предотвратив переполнение на любых разрешениях.

2. **Устранение артефактов бордеров в «Мастере быстрого подключения Prom.ua»**:
   - Сбросить нативные браузерные стили кнопки для заголовка мастера (`bg-transparent border-0 outline-none w-full text-left`), исключив появление дефолтного серого браузерного бордера кнопки.
   - В свёрнутом состоянии (`isOpen === false`) отключать `border-bottom` у `.card__header`, предотвращая паразитный двойной бордер на дне пустой карточки.

3. **Исправление странных бордеров вокруг бейджа «ДОСТУПНО ДЛЯ ПОДКЛЮЧЕНИЯ» и линии снизу таб-бара**:
   - Убрать `border: 1px solid var(--border-subtle)` у `.badge--subtle`, сделав его чистым мягким бейджем без рамок (единообразно с `.badge--emerald` и остальными).
   - Убрать оторванную разделительную линию `border-b border-subtle pb-3` у `#platform-tabs` в `ClientPortalPage.tsx`.

4. **Предотвращение распирания карточек длинными строками (URL фида, вебхука)**:
   - Добавить `min-w-0` к блокам и flex-элементам с `truncate` (`prom-feed-url`, `prom-webhook-url`), чтобы ссылки сжимались с эллипсисом (`...`) без выхода за пределы карточки.

5. **Устранение сбитого текста и нехватки отступов в модальных окнах (`ExportCompletedScreen`, `ImportCompletedScreen`)**:
   - Реализовать класс `.warning-badge-glow` со светящимся янтарным бейджем 68×68px и центровкой `margin: 0 auto;`.
   - Ввести недостающие CSS-утилиты (`space-y-1`, `space-y-5`, `space-y-6`, `mx-auto`, `max-w-md`, `leading-relaxed`, `mb-5`).
   - Настроить четкие вертикальные отступы между иконкой, заголовком, текстом описания и сеткой карточек.

6. **Обновление базы знаний и правил агентов**:
   - Зафиксировать правила в `patterns_and_pitfalls.md` и `AGENTS.md`.
